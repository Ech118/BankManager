# P1 (data/, mcp_server/, fixtures/real/) dependencies.
# Owned by P1. Never create a root requirements.txt (CONTRIBUTING.md).
-r ../schema/contracts/requirements.txt

httpx>=0.27          # EDGAR and market clients, with timeouts and retries
psycopg[binary]>=3.1 # Postgres store (MODE=live only; mock mode needs no DB)
lxml>=5.2            # filing HTML -> structured sections
python-dateutil>=2.9 # fiscal period arithmetic
