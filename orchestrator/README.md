# orchestrator

**Owner: P3 (Agents, Orchestrator & Web).** Only the owner edits this directory (plan.txt 15.4).

Pipeline runner, FastAPI server (POST /api/analyze, SSE events, GET run). Public interface: api.py.
