# Contract-test dependencies. Installed by `make setup`.
# pydantic: the contracts themselves (schema/contracts/) are pydantic models.
# jsonschema + referencing: validate fixtures against the GENERATED schema/*.json,
# so the committed JSON is exercised and not just the Python models.
pydantic>=2.7,<3
jsonschema>=4.18
referencing>=0.30
pytest>=7
